package com.example.androidkalshibot.ui

// This file's content moved 2026-08-04 into MainScreen.kt (tab host),
// OverviewTab.kt, PositionsTab.kt, HistoryTab.kt, and Common.kt (shared
// helpers) as part of splitting the single scrollable dashboard into tabs.
// Kept as an empty file rather than deleted so the module structure stays
// obvious; safe to remove entirely.
